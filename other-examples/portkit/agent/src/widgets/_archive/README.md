# Archived widget descriptors

These files were previously canonical examples in the default domain but
are no longer registered. Kept here for reference. They reference the
`copilotkit://app-dashboard-catalog` catalog ID and a former version of
`agent/src/a2ui_fixed_schema.py` which has been deleted.

The current canonical "copy this first" example is
`agent/src/tools/risk_register.py`.
